
<div class="row">
	<h1><%= @post.title %></h1>
</div>

<!--
<% if false %>
 <div class="row">

	<% @post.pictures.each do |pic| %>
		<%= image_tag pic.image.url, class: "img-responsive col-md-4", style: "padding: 10px" %>
	<%  end %>
</div>
<% end %>
 -->


<div class="row">
	<p><%= raw @post.body %></p>
	<p>Posted by <%= link_to @post.user.name, @post.user %></p>

	<% if correct_user? @post.user %>
		<%= link_to "Edit", edit_post_path %>
		<%= link_to "Delete", post_path, method: :delete %>
	<% end %>
</div>

<div class="row">

	<h3>Comments</h3>

	<%= render 'comments/form' %>

	<% @comments.each do |comment| %>
		<%= render comment %>
	<% end %>
	<%= will_paginate @comments, renderer: BootstrapPagination::Rails %>

</div>
